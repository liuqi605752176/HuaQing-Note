要求：  

不要抄；
可以讨论，但不要做拿来主义；


（1）移植自己做手册
（2）每个人发日报（两周）： 邮件发给吴，抄张雅琴；如有驱动，抄田怀志；
内容： 今天完成了什么，遇到什问题，解决了什么，如何解决的，没解决什么；
（3） 组长每周周报   组员每人完成进度，目前困难；两周后，组项目日报；





A. 移植：


系统移植四要素：  （BSP  板级支持包）
(1）交叉编译器  
a9： arm-none-linux-gnueabi
mips?  openwrt
(2) BootLoader u-boot   bootcmd tftp bootm bootargs
(3) 内核kernel   uImage
(4) 根文件系统   busybox


1. 配置板级支持包（不建议重新编译uboot和文件系统）

web，Android，qt组： 交叉编译器，内核文件（linux.3.14-fs4412.tar.gz）
make menuconfig --> make uImage， device tree
根文件系统  rootfs

openwrt组 ： 下载openwrt源码，交叉编译器 配置虚拟机；

2. usb  --》 鼠标，摄像头，usb串口

配置内核，配置设备树；

移植usb串口(看芯片 ch340) --》 /dev/ttyUSB0  --》 上位机，下位机通了

3. 移植摄像头    --》 能找到 /dev/video0 设备节点

v4l  video for linux 框架

4. 软件和库支持

（1）sqlite3 
（2）摄像头  移植mjpeg-streamer（内置视频服务器 通过网页验证视频移植是否成功）   libjpeg

qt组： 移植屏幕，鼠标，qt
web组：移植boa
openwrt组 ：  软件移植 依靠包管理

5. 整合（配合其他成员）

主程序，驱动，下位机，界面

B. 主程序

安卓组  tcp服务器架构  c/s  多进程实现（tcp服务器和其他部分）
要和安卓界面进行tcp通信，用驱动进行控制，下位机进行串口通信

qt组  多线程服务器（和qt通信）
qt界面和主程序通信，控制驱动，串口连下位机

web组 多线程服务器
和cgi程序通信，控制驱动，和下位机串口通信

openwrt组 小车同安卓组，智能家居 多线程服务器
和界面通信（cgi或者安卓），无驱动（gpio尝试一下），下位机通信（串口，或者esp8266）

C.驱动  建议：platform总线，输入子系统，sysfs

1. led （室内灯）
2. 蜂鸣器
3. ds18b20 （室内温度）
4. mpu6050


D. 下位机  stm32f103rct6

1. 基本使用 keil5
2. led
3. 串口使用点灯   《--  主程序下达命令
4. dht11 上传数值   《--》  主程序读温度   （室外温湿度）
    ： 熟悉单总线模块的使用
5. adc使用    MQ模块     ao口

6. 其他模块 人体红外，火焰，光敏等

推荐： 有stm32基础的同学 尝试用freertos写

注： openwrt组  使用esp8266模块（主程序加tcp）和 主板通信  

E. 界面

(1） 安卓：  注册，登录，点灯（控制其他设备），查温湿度，其他传感器数值

设置阈值（自动报警），摄像头监控画面，抓拍（存在安卓手机）。

(2)  qt:   注册，登录，点灯（控制其他设备），查温湿度，其他传感器数值

设置阈值（自动报警），摄像头监控画面。

注： 小键盘，曲线图（qcustomerplot），抓拍（存在a9内）

(3)  web（html 和 cgi）:   注册，登录，点灯（控制其他设备），查温湿度，其他传感器数值

设置阈值（自动报警），摄像头监控画面。

注： 抓拍（存在a9内）


(4) openwrt web和安卓

注册，登录，点灯（控制其他设备），查温湿度，其他传感器数值

设置阈值（自动报警），摄像头监控画面。